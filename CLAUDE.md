# 改修フロー（必須）

コードを改修したら、人間に提示する前に必ず評価サブエージェントによるチェックを行い、合格するまで自己ループで修正すること。

## 手順

1. **改修を実施**（Edit / Write / Bash 等）。
2. **評価サブエージェントを起動**（`Agent` ツール、subagent_type は `general-purpose` を基本とし、コードベース横断調査が必要な場合は `Explore`。**Playwright 確認が必要な改修では `general-purpose` 固定**）。プロンプトには以下を含める：
   - 変更したファイルパスと変更内容の要約
   - 改修の意図・受け入れ基準（ユーザー要望、関連する受け入れ条件）
   - チェック項目（3 点）：
     - 受け入れ基準を満たしているか
     - 既存テスト・静的解析への影響（`composer.json` の `scripts` に定義されたもの＝phpunit / phpstan / psalm / php-cs-fixer / rector 等を対象）
     - セキュリティ・周辺整合性（SQL/XSS/CSRF/権限、命名規約、デッドコード、過剰抽象化）
   - **Playwright MCP による実機確認**（必須・後述「Playwright 確認」参照）
   - **指示**：「合否判定（PASS / FAIL）と FAIL の場合は具体的な修正指示を返せ。Playwright で確認した URL・操作・観測結果を必ず含めること」
3. **FAIL なら修正してステップ 2 へ戻る**。次のいずれか早い方でループを止め、状況をユーザーに相談する：
   - 同一ファイル × 同一指摘カテゴリで 2 回連続 FAIL
   - 通算 3 ループ到達
4. **PASS したら人間に提示**。冒頭に明記：
   - 「評価エージェントで PASS 確認済み」
   - 評価者の主な指摘 1〜2 行（修正済み含む）

## Playwright 確認（評価エージェントが実施）

評価エージェントは `mcp__playwright__browser_*` ツール群を使い、改修箇所の実動作をブラウザで確認する。型チェックや単体テストでは検出できないテンプレ崩れ・JS エラー・回帰を捕まえるのが目的。

### 前提

- ベース URL：`http://localhost:9080`（renewal ブランチの docker、ポート 9080）
- ログイン：`/ui/login` で `rucaro` / `rucaro2026`（要再確認時はユーザーに聞く）
- dev サーバ未起動なら `docker compose up -d` を促す（評価エージェントが勝手に立ち上げない）

### 何を確認するか（改修種別ごと）

- **UI（テンプレ / JS / コントローラ）改修**：該当ページを開き、改修した操作を実行し、`browser_snapshot` または `browser_console_messages` で結果と JS エラー有無を確認
- **ドメイン / インフラ / リポジトリ改修**：その変更が露出する画面（例：仕訳保存→`/ui/journals/show/...`、TB 計算→`/ui/trial-balance`）まで辿り、想定値が表示されることを確認
- **CLI / バッチ / マイグレーション**：UI に出る副作用（例：再インポート後の TB 値）を 1 画面で確認

### Playwright 確認をスキップしてよい場合

- 純粋にユーザーから見えない内部リファクタ（テストで等価性が担保されている）
- ドキュメント・コメントのみの変更
- 評価エージェントが「dev サーバ未起動」「ログイン情報不明」等で確認不能と判断 → ユーザーに状況報告して停止

## スキップ条件（評価エージェント自体が不要）

以下のいずれかに該当する場合のみスキップ可。スキップしたことを提示時に明示する。

- コード改変なし（調査・読み取り・質問回答のみ）
- 1 ファイル変更かつ 10 行未満かつテスト・本番ロジック未変更（typo / コメント / 純粋なフォーマット）
- ユーザーが「評価不要」「そのまま見せて」と明示した
